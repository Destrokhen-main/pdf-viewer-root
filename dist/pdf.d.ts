import "pdfjs-dist/legacy/build/pdf.worker.entry.js";
import { PDFDocumentProxy } from "pdfjs-dist/types/src/display/api";
export declare class PdfController {
    private wrapper;
    private loading;
    private onError;
    private onSuccess;
    private logger;
    private pdfBuffer?;
    pdfBlob?: Blob;
    pdf?: PDFDocumentProxy;
    private url?;
    private preFrame?;
    showAll: boolean;
    private scale;
    dpi: number;
    private mainFrameElement;
    firstPage: number;
    private deb;
    constructor(wrapper: HTMLElement, loading: HTMLElement, onError: (e: any) => void, onSuccess: (pdf: PDFDocumentProxy) => void, logger: (...args: string[]) => void);
    init(url: string): void;
    fetchData(): Promise<void>;
    fetchPdf(): void;
    initPages(): void;
    schedular(frameId?: number): void;
    renderPdf(num?: number): Promise<void>;
    renderPerPagePdf(num?: number): Promise<void>;
    addPrintStyles(iframe: any, sizeX: any, sizeY: any): void;
    createPrintIframe(node: HTMLElement): Promise<unknown>;
    changeMod(mode: number, page?: number): void;
    clear(): void;
    abort(): void;
    rerender(num?: number): void;
}
